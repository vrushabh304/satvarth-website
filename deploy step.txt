### Install WSL properly
wsl --install
 
 
 
 wsl
sudo apt update
sudo apt upgrade -y
sudo apt install python3 python3-venv python3-pip -y
mkdir -p ~/project
cp -r /mnt/e/project/* ~/project/
cd ~/project
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip freeze > requirements.txt (create requirement file)
pip install -r requirements.txt
pip install gunicorn
gunicorn --bind 0.0.0.0:8000 app:app       
    if 8000 alredy run so check: sudo lsof -i :8000
    so this kill : sudo kill -9 1234(1234 is pid number)
    and return start bind
http://localhost:8000

### Install Nginx

sudo apt install nginx -y
sudo service nginx start
sudo nano /etc/systemd/system/flask.service
            in this 
                [Unit]
                Description=Gunicorn service for Flask app
                After=network.target
                [Service]
                User=vrushabh03
                Group=www-data
                WorkingDirectory=/home/vrushabh03/project
                ExecStart=/home/vrushabh03/project/.venv/bin/gunicorn \
                    --workers 3 \
                    --bind unix:/run/gunicorn/gunicorn.sock \
                    app:app
                RuntimeDirectory=gunicorn
                RuntimeDirectoryMode=0755
                [Install]
                WantedBy=multi-user.target

sudo systemctl daemon-reload
sudo systemctl restart flask
sudo systemctl status flask
    sudo systemctl status flask if not working 
        ls -ld /home/vrushabh03
        ls -ld /home/vrushabh03/project
        ls -ld /home/vrushabh03/project/.venv/bin/gunicorn
        sudo nano /etc/systemd/system/flask.service
sudo systemctl daemon-reload
sudo systemctl restart flask
sudo systemctl status flask (if =sucessful)

sudo nano /etc/nginx/sites-available/flask
sudo ln -s /etc/nginx/sites-available/flask /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
sudo service nginx restart

ls -l /etc/nginx/sites-enabled/ (in this only flask defult was remove)
        remove default
        ls /etc/nginx/sites-enabled
        sudo rm /etc/nginx/sites-enabled/default
        sudo nginx -t
            check ok: [nginx: the configuration file /etc/nginx/nginx.conf syntax is ok]
            check successful :[ nginx: configuration file /etc/nginx/nginx.conf test is successful]
        sudo mv /etc/nginx/sites-available/default /etc/nginx/sites-available/default.disabled
        ls /etc/nginx/sites-enabled (now check only flask)
        curl -I http://localhost
        sudo cat /etc/nginx/sites-available/flask
        curl --unix-socket /run/gunicorn/gunicorn.sock http://localhost/
        paste in chrome and check it "http://localhost/" working or not

groups vrushabh03

172.27.96.1
# add ngrok apt repo & install
curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
sudo apt update
sudo apt install ngrok -y
ngrok config add-authtoken 36NHoa5NNp3rSWI1ZnV1WC0eFOn_2mqS9MQdHvotrsnBJzttH

http://192.168.1.45